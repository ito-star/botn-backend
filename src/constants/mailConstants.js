import { appName } from "../constants/globalConstants"
export const verifyEmailSubject = appName + " user signup verification"
export const forgotPasswordEmailSubject = "[" + appName + "] password reset link"

